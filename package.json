{
    "name": "elmtest",
    "private": true,
    "version": "0.0.0",
    "type": "module",
    "scripts": {
        "dev": "npm run watch & vite dev",
        "build": "vite build",
        "preview": "vite preview",
        "watch": "chokidar --silent 'src/*.elm' -c 'elm make src/*.elm --output=elm.js'"
    },
    "devDependencies": {
        "chokidar-cli": "^3.0.0",
        "vite": "^5.2.0"
    }
}
